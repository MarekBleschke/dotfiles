services:
  dotfiles-test:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/dotfiles:ro
      - ./mock-profile:/mock-profile:ro
    environment:
      - HOME=/home/testuser
    working_dir: /home/testuser
    stdin_open: true
    tty: true
